#include "main.h"

/**
 * reverse_array - a function that reverses an array.
 * @a: an input array
 * @n: number of elements of array
 * Return: Nothing
 */

old reverse_array(int *a, int n)
{
	int = 0;
	int aux;

	while (i < n--)
	{
		aux = a[i];
		a[i++] = a[n];
		a[n] = aux;
	}
}
